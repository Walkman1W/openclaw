[project]
name = "openclaw"
version = "0.1.0"
description = "OpenClaw Agent Labor Market Platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.coverage.run]
source = ["app"]
omit = ["tests/*"]
